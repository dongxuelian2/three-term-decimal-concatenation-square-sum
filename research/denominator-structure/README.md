# Denominator structure

## Problem

Study the denominator kernel, decimal interfaces, source phase, orientation,
and DD reductions arising before the A1 branch.

## Status

partial / open

## Parent

Parent: [`../exact-lift/`](../exact-lift/)

## Established results

The local records include error closure, independence, orientation recovery,
tail-window, source-phase, and supply-synchronization reductions.

## Closed routes

The archive records the D-prime equal-load and D-split firewall analysis. Its
scope is the stated DD region; it is not a global impossibility result.

## Historical aliases

The former `20-DD` and `DD` paths are listed in the migration aliases.
