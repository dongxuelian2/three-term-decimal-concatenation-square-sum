# Moving-profile state

These state and continuation documents are snapshots of the active boundary,
not independent theorem claims. Read their scope and evidence sections before
using them as dependencies.
