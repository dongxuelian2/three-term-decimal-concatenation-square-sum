# A1 route records

This directory holds older A1 route reports and checkpoints whose mathematical
parent is A1 but whose narrower node was not confidently recoverable from the
corpus. They remain preserved as attempts or failures according to the migration
metadata; no route is silently promoted to a theorem.
